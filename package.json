{
  "name": "jinbaoweb.github.io",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "next build && next export && touch out/.nojekyll && git add . && git commit -m 'deploy' && git push origin gh-pages"
  },
  "dependencies": {
    "@next/mdx": "^14.2.0",
    "@tailwindcss/typography": "^0.5.10",
    "next": "14.2.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "tailwindcss": "^3.4.3"
  }
}
